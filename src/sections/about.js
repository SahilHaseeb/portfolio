/**
 * about.js — About section module
 *
 * Currently: no JS needed — the section is fully CSS-driven.
 * This file is a stub for future enhancements (e.g. animated
 * counters, skill percentage reveals, etc.).
 *
 * Export kept consistent with the rest of the architecture
 * so main.js can import it uniformly.
 */

export function initAbout() {
  // Section works purely with CSS + existing IntersectionObserver
  // from animations.js (fade-up / slide-in classes).
  // Nothing to initialise for Step 4.
}
