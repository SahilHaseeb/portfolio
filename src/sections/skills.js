/**
 * skills.js — Skills section module
 *
 * Currently: no JS required — the section is fully CSS-driven.
 * Skill cards use CSS hover/focus effects and the shared
 * IntersectionObserver from animations.js handles reveal.
 *
 * Stub exported consistently for main.js import pattern.
 */

export function initSkills() {
  // Skill cards are interactive via CSS only (hover, focus-within).
  // Scroll reveal handled by the global initScrollAnimations().
  // Nothing to initialise for Step 4.
}
